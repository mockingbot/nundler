# check yaml syntax summary from here: http://yaml.org/refcard.html
# check compiled yaml output: http://nodeca.github.io/js-yaml/
# check yaml doc: https://www.google.com/

version: 2

jobs:
  build:
    docker:
      - image: circleci/node:10

    steps:
      - checkout

      - run:
          name: <Setup> javascript
          command: npm ci

      - run:
          name: <Test> javascript
          command: npm test
